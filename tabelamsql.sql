create database survivor;

use survivor;

create table usuario (
idUsuario int primary key,
nome varchar (45),
email varchar (45),
senha password varchar (20),
usuario varchar (20));

create table ong (
idOng int primary key),
;